# Все ключевые значения захардкожены прямо в коде.
# Этот файл оставлен для справки — переменные на хостинге добавлять НЕ нужно.
#
# RESEND_API_KEY  → захардкожен в app/api/contact/route.ts
# CONTACT_EMAIL   → захардкожен в app/api/contact/route.ts  (info@stroygenpodriad.ru)
# NEXT_PUBLIC_SITE_URL → захардкожен в layout.tsx, sitemap.ts, robots.ts (https://stroyfintrust.ru)
#
# Если в будущем понадобится сменить почту или домен — меняйте только в указанных файлах.
